# BigWigs Packager resolves and populates Libs/ at build time — never commit
Libs/

# Packager build output
.release/
*.zip

# Internal project notes — roadmap, design rationale, working memory.
# Kept out of the public repo; ADRs under docs/adr/ cover the formal
# decisions that anyone reading the codebase needs.
CLAUDE.md

# Editor/OS noise
.claude/
.vscode/
.idea/
*.bak
*.swp
*.swo
.DS_Store
Thumbs.db

# Lua bytecode (shouldn't exist in this repo, but just in case)
*.luac
